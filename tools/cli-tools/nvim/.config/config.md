# File Structure

This is how the file structure looks like

```bash
/Users/parth.gupta/.config/nvim % ls
init.lua  lazy-lock.json  lua
```


everything is inside of the .config/nvim

I will paste all of these files in the .config folder.
